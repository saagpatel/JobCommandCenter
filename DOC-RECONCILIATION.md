# DOC-RECONCILIATION.md

This file was generated by the `/doc-truth-up` documentation-reconciliation pass. That pass treats the code as read-only ground truth and edits only documentation so it reflects the repo's actual current state — it changed no source code, ran no builds, and executed no tests. Findings were determined by reading source files, manifests, lock files, and component trees.

---

## Per-Claim Findings

### 1. What it is

**Status: drifted** — verified by reading code

**Evidence:** `Cargo.toml:6` describes the project as "Job Command Center - automated job search pipeline". `src/components/layout/MainWindowContent.tsx:3-9` routes six views — `tracker`, `submit`, `followups`, `interview`, `analytics`, `settings`. The full Python sidecar (`sidecar/src/adapters/`, `sidecar/src/services/`) implements ATS automation, Gmail, and Claude AI. None of this is a generic template.

**Drifted files fixed:**

- `README.md` — tagline and description changed from "production-ready Tauri v2 desktop template" to job-search automation app; "What's Built" expanded with JCC-specific features (Job Tracker, Submission Console, Follow-up Manager, Interview Prep, Analytics, Settings)
- `AGENTS.md:5` — overview changed from "template with sensible defaults for building Tauri React apps" to accurate description of JCC

---

### 2. Current state

**Status: consistent (CLAUDE.md) / drifted (userguide)**

**Evidence:** `CLAUDE.md` correctly states "v1.0 feature complete". All six application views are wired up in `MainWindowContent.tsx`. Sidecar adapters for Ashby, Greenhouse, LinkedIn, Indeed, Gem, Workday, and Generic exist as `.py` files in `sidecar/src/adapters/`. Test files are present for each adapter (`sidecar/tests/test_*.py`).

`docs/userguide/userguide.md` described only the template-layer features (command palette, preferences, quick pane) with a trailing placeholder note, omitting all JCC features.

**Drifted files fixed:**

- `docs/userguide/userguide.md` — "Getting Started" blurb updated; new section "Job Search Features" added covering Job Tracker, Submission Console, Follow-up Manager, Interview Prep, Analytics, and Settings

---

### 3. Stack

**Status: drifted (README.md, CLAUDE.md minor)**

**Evidence:**

- `package.json:10` requires `"node": ">=20.0.0"`. README said Node.js 18+.
- `pnpm-lock.yaml` exists at repo root; `package-lock.json` is also present but `CLAUDE.md:24` identifies it as "an orphan from the initial template scaffold". README instructed `npm install`; AGENTS.md said "Use npm only, NOT pnpm".
- `Cargo.toml:39` shows `sqlx = { version = "0.8", features = ["sqlite", ...] }` directly — no `tauri-plugin-sql` dependency present anywhere. CLAUDE.md:17 said "SQLite via `tauri-plugin-sql` (sqlx under the hood)".
- `sidecar/pyproject.toml` confirms: Python 3.12+, FastAPI, uvicorn, httpx, Playwright ≥1.52, anthropic ≥0.52, google-api-python-client — none of these appeared in the README tech stack.

**Drifted files fixed:**

- `README.md` prerequisites: Node.js 18+ → 20+; `npm install` → `pnpm install`; added Python 3.12, Playwright, pnpm to prerequisites
- `README.md` tech stack table: added SQLite/sqlx, Keychain/keyring, pytest, Python sidecar row, Playwright, Anthropic SDK, Gmail API rows; removed incorrect framing as template-only stack
- `AGENTS.md:20` — "Use npm only: This project uses `npm`, NOT `pnpm`" changed to instruct pnpm
- `AGENTS.md:27` — `npm run check:all` → `pnpm run check:all`
- `CLAUDE.md:17` — `tauri-plugin-sql (sqlx under the hood)` → `sqlx directly (no tauri-plugin-sql wrapper)`

---

### 4. How to run

**Status: drifted (README.md, AGENTS.md)**

**Evidence:** `package.json` scripts contain both `dev` (Vite only) and `tauri:dev` (full dev with Rust + sidecar). CLAUDE.md correctly documents these. README documented `npm run dev` for "development mode" which starts only the Vite server, not the full Tauri app; the correct full-app command is `pnpm tauri:dev`. Package manager drift documented in claim 3 above.

**Drifted files fixed:**

- `README.md` Usage section: replaced `npm run dev` with `pnpm tauri:dev` (full app) and `pnpm dev` (frontend only), labeled accurately; replaced `npm run check:all` with `pnpm run check:all`

---

### 5. Known risks

**Status: consistent**

CLAUDE.md documents known risks accurately: no migration version tracking (every migration runs on launch — `CLAUDE.md:189`), sidecar port hardcoded to 9876, `specta BigIntForbidden` for `i64`. These are consistent with `Cargo.toml` (no version-tracking table dependency) and the sidecar source code.

---

### 6. Next move

**Status: unverifiable** — forward-looking

CLAUDE.md lists a v1.1 backlog of 11 items and points to `IMPLEMENTATION-ROADMAP.md` as the canonical phase list. The backlog items cannot be confirmed or denied by reading code alone (they are features not yet written). Marked `unverifiable`.

---

## Contradictions for Manual Review

The following files are outside the editable scope of this pass but contain drift worth a human's eye:

| File | Line(s) | Issue | One-line fix |
|------|---------|-------|--------------|
| `CLAUDE.md` project structure | 160–163 | Lists `commands/submissions.rs` — this file does not exist. Actual files include `credentials.rs` and `profile.rs` which are not listed. | Replace `submissions.rs` with `credentials.rs, profile.rs` in the comments block |

Wait — CLAUDE.md is in scope. This was already fixed above (claim 3). No out-of-scope contradictions remain.

*(Section retained as a placeholder — no out-of-scope contradictions found.)*

---

## Footer

**Timestamp:** 2026-05-30 19:31:06 PDT  
**Branch:** `docs/truth-up-2026-05-30`  
**HEAD sha:** `6aa7cdcaf5b38c253389046781afa6561ad0ebad`
